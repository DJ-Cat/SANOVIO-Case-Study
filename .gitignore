node_modules/
.next/
.next-demo/
out/
*.tsbuildinfo
next-env.d.ts
.env
.env.local
db/*.db
# The demo edition's starting point ships with the repo, so a fresh checkout
# can run `npm run demo` and see the platform filled. See scripts/demo.ts.
!db/demo-snapshot.db
db/*.db-journal
db/*.db-shm
db/*.db-wal
.DS_Store

# graphify
graphify-out/

# extract_lib
extract_lib/.venv/
extract_lib/out/
extract_lib/.env
extract_lib/**/__pycache__/

# Frozen catalogue captures. Real manufacturer PDFs and product photography —
# somebody else's copyrighted material, so it stays out of the repo. Recreate
# with `npm run demo:export` after an extraction, or commit deliberately.
fixtures/*/
